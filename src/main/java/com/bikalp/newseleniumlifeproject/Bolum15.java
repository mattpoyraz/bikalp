package com.bikalp.newseleniumlifeproject;


import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

import io.github.bonigarcia.wdm.WebDriverManager;
import static org.openqa.selenium.support.locators.RelativeLocator.with;

public class Bolum15 
{
    public static void main( String[] args ) throws InterruptedException
    {
        /* 
        WebDriverManager.chromedriver().setup();
	    WebDriver driver = new ChromeDriver();
        driver.get("https://rahulshettyacademy.com/angularpractice/");
        
       WebElement editBox=driver.findElement(By.cssSelector("[name='name']"));

       driver.findElement(with(By.tagName("label")).above(editBox)).getText();
       driver.findElement(with(By.tagName("label")).below(editBox)).getText();
       driver.findElement(with(By.tagName("label")).toleftof(editBox)).getText();
       driver.findElement(with(By.tagName("label")).torightof(editBox)).getText();

        */
    }
}
